class PointLightSource {
  constructor(diffuse, ambient, specular, lightPos) {
    this._diffuse = diffuse;
    this._ambient = ambient;
    this._specular = specular;
    this._lightPos = lightPos;
  }
}
